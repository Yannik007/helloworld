<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.entity.User">
<select id="selectUser" resultType="map">
    select * from user where id = #{id}
</select>
</mapper>
